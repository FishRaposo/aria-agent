# Base URL of the ARIA FastAPI backend.
# The dashboard is live-first: it calls this URL and, only on a network failure
# (backend down/unreachable), falls back to bundled demo data with a visible
# "Demo mode" badge. Real HTTP 4xx/5xx are surfaced as error states.
NEXT_PUBLIC_API_URL=http://localhost:8000
