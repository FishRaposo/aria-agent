# Execution plan (historical record)

The original single-step loop, approval queue, persistence fallback, cost
tracking, tracing, and progressive-disclosure skill layer are preserved behind
compatibility facades. The finalization pass added the internal vendored core,
bounded multi-hop execution, safety/retry/rate policies, SSE, replay, local
memory search, and deterministic evidence.

Use `make evidence` and the commands in `AGENTS.md` as the current execution
contract. Do not treat old sibling-package references in archived notes as
installation instructions.
